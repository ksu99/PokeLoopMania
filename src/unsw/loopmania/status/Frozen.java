package unsw.loopmania.status;

public class Frozen extends Stunned {
    
    /**
     * Frozen has the same fundamental effects as stunned but with a different name
     */
    public Frozen() {
        super();
        setID("Frozen");
    }
}
