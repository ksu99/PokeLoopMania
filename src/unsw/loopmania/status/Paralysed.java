package unsw.loopmania.status;

public class Paralysed extends Stunned {
    
    /**
     * Paralysed has the same fundamental effect as stunned, but has a different name
     */
    public Paralysed() {
        super();
        setID("Paralysed");
    }
}
